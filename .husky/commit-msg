#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

pnpm exec commitlint --edit $1

if [ $? -ne 0 ]; then
  cat << 'EOF'

🧙‍♀️ 올바른 커밋 메시지 형식:

  <type>: <subject>

허용 타입:
  • feat:     새로운 기능
  • fix:      버그 수정
  • refactor: 코드 리팩토링
  • style:    코드 포맷팅, 세미콜론 등
  • docs:     문서 수정
  • test:     테스트 추가/수정
  • chore:    빌드 설정, 패키지 관리 등
  • perf:     성능 개선

예시:
  feat: 사용자 로그인 기능 추가
  fix: 이미지 업로드 시 에러 수정
  chore: Husky 커밋 컨벤션 설정

EOF
  exit 1
fi